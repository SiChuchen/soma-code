/**
 * Recovery placeholder for generated SDK tool types.
 *
 * The public SDK surface re-exports this module, but the extracted snapshot
 * does not include the original generator inputs. Keep the module present so
 * downstream imports resolve while the runtime-facing definitions live in
 * runtimeTypes.ts.
 */
export {}
