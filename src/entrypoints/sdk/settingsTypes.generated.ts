/**
 * Recovery placeholder for generated settings types.
 *
 * The extracted snapshot is missing the original generator inputs, so the
 * rebuilt workspace keeps the public surface permissive until the concrete
 * schema can be regenerated.
 */
export type Settings = Record<string, unknown>
