# somacode API

This recovered placeholder keeps the bundled `claude-api` skill loadable in
the extracted snapshot.

## Reading Guide

The original bundled markdown pages for this skill were not present in the
local extraction. Treat the inline reference pages as placeholders only.

## When to Use WebFetch

If the bundled placeholder pages are not specific enough, fetch the latest
official Anthropic documentation before giving implementation guidance.
