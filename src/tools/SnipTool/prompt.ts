export const SNIP_TOOL_NAME = 'Snip'

export const DESCRIPTION = 'Trim older conversation context from the active working set'

export const PROMPT = `Use this tool to remove older, no-longer-relevant context from the active conversation working set.

This reconstructed snapshot currently provides only a disabled compatibility stub, so expect an unavailable result until the full snip implementation is restored.`
