export const TERMINAL_CAPTURE_TOOL_NAME = 'TerminalCapture'

export const DESCRIPTION =
  'Inspect recent output from the built-in terminal panel'

export const PROMPT = `Use this tool to inspect recent output from the built-in terminal panel without launching a shell command.

This reconstructed snapshot currently provides only a disabled compatibility stub, so expect an unavailable result until the full terminal panel capture flow is restored.`
