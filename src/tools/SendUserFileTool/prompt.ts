export const SEND_USER_FILE_TOOL_NAME = 'SendUserFile'

export const DESCRIPTION = 'Send a file to the user'

export const PROMPT = `Send a file to the user when the result is best represented as an attachment instead of inline text.

Use this for artifacts the user should open directly, such as logs, generated outputs, or other files already present in the workspace.`
